/**
 * Null-marked: every type in this package is non-null unless explicitly annotated
 * {@link org.jspecify.annotations.Nullable}. Record components in particular are never
 * null — a component that may be absent is declared {@code Optional<T>} instead.
 */
@NullMarked
package dev.bluestep.global.dto.dsm5updater;

import org.jspecify.annotations.NullMarked;
